# Keep the pinentry choice in a user-owned wrapper so terminal commands can
# continue using pinentry-curses while graphical applications use GTK.
pinentry-program /home/jota/.local/bin/pinentry-smart
